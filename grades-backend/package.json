{
  "name": "grades-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "npx dotenv-cli -- nodemon src/server.js",
    "test": "dotenv -e .env.test -- jest --runInBand",
    "reset:test": "dotenv -e .env.test -- prisma migrate reset --force --skip-seed",
    "reset:main": "dotenv -e .env -- prisma migrate reset --force --skip-seed",
    "generate": "prisma generate",
    "postinstall": "prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.11.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv-cli": "^8.0.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "sqlite3": "^5.1.7",
    "supertest": "^7.1.3"
  }
}
